

Keywords:
		Search keywords:  multi vmm_group vmm1.2

Technical content:
 
		Title: 		multi class  with vmm_group as base class
 
		Objective:    blueprint replace through mutiple vmm_group derived class 

		Description:   This test shows the usage replacing the vmm_factory class through the mutiple group derived class 

		Additional setup: None	
 
 To run the example:
      Using Makefile:
       	clean           -- To clean up the log files and executable of previous run
       	comp            -- To compile the design
        run             -- To run the design
        all             -- It does all the above steps.
     Using shell scripts:
        clean.csh       -- To clean up the log files and executable of previous run
        comp.csh        -- To compile the design
        run.csh         -- To run the design  
 
 
Location Information:
		PATH: 			$VMM_HOME/examples/testbench/sv/vmm/std_lib/factory_pattern/multi_intance_gen
		DIAGRAMS: 		None
		Cross Reference:	$VMM_HOME/doc/vmm_user_guide.pdf
